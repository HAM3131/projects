{
  "name": "world-builder",
  "version": "1.0.0",
  "description": "D&D Worldbuilder backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd map && npm start",
    "client-admin": "cd map-admin && npm start",
    "build-client": "cd map && npm run build && cp -r build/* ../public/map",
    "build-client-admin": "cd map-admin && npm run build && cp -r build/* ../public/map-admin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "expres": "^0.0.5",
    "express": "^4.19.2",
    "mariadb": "^3.3.0",
    "mysql2": "^3.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}